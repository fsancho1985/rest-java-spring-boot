spring:
  datasource:
#    dbcp2:
    url: jdbc:mysql://localhost:3306/rest_api_spring_boot_java?useTimezone=true&serverTimezone=UTC
    username: root
    password: senha
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
      show-sql: false

